networks:
  ocr-app:

services:
    frontend:
      build:
        context: .
        dockerfile: Dockerfile
      networks: ['ocr-app']
      ports:
        - 5103:3000                  # Change to your preferred local:container port
      env_file:
        - .env